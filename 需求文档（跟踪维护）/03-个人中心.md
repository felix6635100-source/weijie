---
创建日期: Tuesday, April 28th 2026, 12:23:08 am
修改日期: Wednesday, April 29th 2026, 9:54:23 pm
完成状态:  
tags: [睡眠优等生, 1A2B]
---

# 唯戒 · 个人中心模块需求文档

> 文档版本：v2.0  ｜ 维护人：产品+设计  ｜ 最近更新：2026-04-27
> 设计规范：见《00-设计规范.md》
> 写作模板：每个页面 / 弹层按 **状态 → 元素构成 → 操作逻辑 → 业务规则** 四段式描述。

---

## 0. 模块概览

「个人中心」是承载账号体系、登录注册、个人资料维护、隐私设置的全局模块。入口位于：

- App 启动后未登录态 → 任意 Tab 的"登录引导卡"或顶部头像 → P1 登录。
- 已登录态 → 一级页面顶部头像 → P5 个人设置。

### 包含的子页面与浮层

| ID             | 名称                | 形式      | 触发方式                              | 原型状态 |
| -------------- | ----------------- | ------- | --------------------------------- | ---- |
| P1             | 登录主页              | 全屏页面    | 未登录态点击"登录引导卡" / 顶部头像              | ⬜    |
| P1.1           | 短信验证码登录           | 子步骤     | P1 默认进入此步骤                        | ⬜    |
| P1.2           | 密码登录              | 子步骤     | P1.1 点击"密码登录"链接                   | ⬜    |
| P2             | 验证码输入             | 子步骤     | P1.1 / P-FORGOT 输入手机号后获取验证码      | ⬜    |
| P3             | 注册 / 登录后设密码       | 子步骤     | 短信验证后未注册 / 忘记密码流程最后一步           | ⬜    |
| P4             | 完善个人资料            | 子步骤     | 注册成功后引导                          | ⬜    |
| P-FORGOT       | 忘记密码流程            | 复用 P2 + P3 | P1.2 点击"忘记密码？"                  | ⬜    |
| P5             | 个人设置              | 二级页面    | 已登录态一级页面顶部头像                     | ⬜    |
| P5.1（编辑）       | 滚轮选择器（年龄/身高/体重）   | 滚轮选择器   | P5 个人资料行点击"年龄 / 身高 / 体重"         | ⬜    |
| P5.2（编辑）       | 头像选择器（5×4 网格）     | 底部弹层    | P5 个人资料行点击"头像" / P4 头像区          | ⬜    |
| P5.3（编辑）       | 单字段编辑面板（昵称等）      | 底部弹层    | P5 个人资料行点击"昵称"                   | ⬜    |
| P5.4（编辑）       | 动态显示范围            | 操作菜单    | P5 动态显示范围行点击                     | ⬜    |
| P5.5（编辑）       | 退出登录二次确认          | 确认框     | P5 底部"退出登录"                       | ⬜    |
| P6             | 连接戒指页             | 二级页面    | 今日首页"立即连接戒指" / P5 我的设备行          | ⬜    |
| P6.1           | 已连接态（设备详情）        | 内嵌区段    | P6 当前账号已绑定戒指                     | ⬜    |
| P6.2           | 搜索 / 配对态          | 内嵌区段    | P6 当前账号未绑定 / 主动移除后                | ⬜    |
| P6.3           | 移除戒指二次确认          | 确认框     | P6.1 点击"移除戒指"                     | ⬜    |
| P-LOGIN-DONE   | 登录成功轻提示           | 全屏轻提示   | P4 完成后 / P2 已注册账号验证通过            | ⬜    |
| P-CONNECT-DONE | 连接成功轻提示           | 全屏轻提示   | P6.2 蓝牙握手成功                       | ⬜    |

> **原型状态约定**：⬜ 未开始 ｜ 🚧 进行中 ｜ ✅ 已完成。
>
> **形式术语**：定义详见《00-设计规范.md》§ 6 全局布局 与 § 8 弹窗系统。

---

## P1. 登录主页

全屏页面（覆盖 Tab Bar），承载登录注册全流程。所有子步骤（P1.1 / P1.2 / P2 / P3 / P4）在同一容器内通过表单切换，共用顶栏。

### 状态

P1 本身只是容器；具体内容由当前激活的子步骤决定（详见各子步骤章节）。

### 元素构成

1. **公共顶栏**：
   - 左：✕ 关闭（默认）/ ‹ 返回（仅在 P2 验证码子步骤显示）。
   - 右：「帮助」文字按钮。
2. **子步骤容器**：根据当前 step 渲染 P1.1 / P1.2 / P2 / P3 / P4 之一。

### 操作逻辑

- 点击 ✕ → 关闭整个登录页，回到调起前的 Tab。
- 点击 ‹ → 仅在 P2 显示，回到上一个子步骤。
- 点击"帮助" → 跳帮助 H5 页。

---

## P1.1. 短信验证码登录（默认子步骤）

App 默认进入的登录入口。

### 状态

| 状态     | 触发条件                       | 视觉                          |
| ------ | -------------------------- | --------------------------- |
| 默认     | 进入 P1                      | 主按钮置灰禁用，提示协议默认勾选            |
| 手机号有效  | 输入 11 位且符合格式 + 协议勾选        | 主按钮高亮可点                     |
| 协议未勾选  | 用户取消勾选                     | 主按钮置灰；点击主按钮触发抖动 + 轻提示       |
| 加载中    | 点击主按钮后等待短信下发              | 主按钮内 spinner，文字保留           |

### 元素构成

1. **标题**："欢迎登录 AIRing"（28/700）。
2. **手机号输入行**：
   - 左：国家区号"+86" + ▾。
   - 中：分隔符。
   - 输入框：11 位数字限制，占位"请输入手机号"。
   - 右：✕ 一键清空（仅有内容时显示）。
3. **提示文案**：灰字"未注册的手机号验证后自动创建 AIRing 账号"。
4. **协议同意行**：
   - 左：方形勾选框（默认勾选）。
   - 文案："我已阅读并同意《用户协议》和《隐私政策》"。
5. **主按钮**（通栏品牌蓝）："获取短信验证码"。
6. **次链接**（居中文字）："密码登录"。

### 操作逻辑

- 点击区号 → 弹国家区号选择器（本期写死 +86，仅展示动效）。
- 输入手机号 → 实时校验格式，主按钮联动启用 / 禁用。
- 点击 ✕ → 清空手机号输入。
- 点击协议勾选行 → 切换勾选态。
- 点击《用户协议》/《隐私政策》 → 跳对应 H5 页。
- 点击主按钮（高亮可点态） → 调用短信下发接口 → 切换到 P2 验证码输入子步骤。
- 点击主按钮（协议未勾选） → 协议条抖动 1 次 + 弹轻提示"请先阅读并同意协议"。
- 点击"密码登录" → 切换到 P1.2 子步骤。

### 业务规则

- 手机号格式：1 开头 11 位数字（13/14/15/16/17/18/19 段）。
- 60 秒内同一手机号最多请求 1 次验证码。

---

## P1.2. 密码登录（子步骤）

### 状态

| 状态     | 触发条件                  | 视觉                |
| ------ | --------------------- | ----------------- |
| 默认     | 从 P1.1 切换进入            | 主按钮置灰禁用            |
| 可登录    | 手机号 + 密码非空 + 协议勾选     | 主按钮高亮可点           |
| 错误     | 提交后服务端返回错误            | 显示红字"手机号或密码错误，请检查后重试" |
| 加载中    | 点击登录后等待响应             | 主按钮内 spinner       |

### 元素构成

1. **标题**："密码登录"。
2. **手机号输入行**（同 P1.1）。
3. **密码输入行**：
   - 输入框：占位"请输入登录密码"，默认隐藏。
   - 右侧 👁 切换显示 / 隐藏。
4. **错误提示行**（红字，仅出错时显示）："手机号或密码错误，请检查后重试"。
5. **协议同意行**（同 P1.1）。
6. **主按钮**（通栏）："登录"。
7. **底部三联次链接**（横排）：
   - 左："验证码登录"。
   - 右："忘记密码？"。

### 操作逻辑

- 输入手机号 / 密码 → 主按钮联动。
- 点击 👁 → 切换密码可见。
- 点击主按钮 → 提交登录；成功 → 弹 P-LOGIN-DONE 轻提示后跳上次所在 Tab；失败 → 显示错误行 + 抖动一次。
- 点击"验证码登录" → 切回 P1.1。
- 点击"忘记密码？" → 进入 P-FORGOT 流程（自动跳到 P1.1 但带"忘记密码"上下文）。

### 业务规则

- 同手机号连续登录失败 5 次 → 锁定 10 分钟，弹轻提示"尝试次数过多，请稍后再试"。

---

## P2. 验证码输入（子步骤）

### 状态

| 状态  | 触发条件         | 视觉                       |
| --- | ------------ | ------------------------ |
| 默认  | P1.1 提交后进入   | 6 格空格 + 倒计时 60 秒          |
| 输入中 | 输入 1~5 位数字   | 已输入格位填值 + 当前格位高亮          |
| 错误  | 输入 6 位但服务端不通过 | 红字"验证码错误，请重新输入" + 自动清空    |
| 倒计时结束 | 60 秒后        | 倒计时变成"重新获取"按钮，可点         |
| 已注册成功 | 6 位正确 + 服务端识别为已注册 | 自动跳走，弹 P-LOGIN-DONE        |
| 未注册成功 | 6 位正确 + 服务端识别为未注册 | 自动切到 P3 设置密码（注册流程）        |

### 元素构成

1. **标题**："输入验证码"。
2. **副标题**：动态显示"验证码已发送至 +86 138 0000 0000"。
3. **6 位验证码格子行**：
   - 6 个等宽方格，每格显示一位数字。
   - 当前焦点格底边高亮。
   - 隐藏的输入框接收键盘输入并自动分配到 6 格。
4. **错误行**（红字，仅出错时）："验证码错误，请重新输入"。
5. **元信息行**（横排）：
   - 左：动态文字"重新获取 (60s)" / 倒计时结束后变成可点按钮"重新获取"。
   - 右：链接"手机号已停用？"。

### 操作逻辑

- 输入数字 → 自动填入下一格。
- 6 位输完 → 自动提交校验。
- 校验错误 → 显示红字 + 自动清空 + 焦点回到第 1 格。
- 倒计时归零后点击"重新获取" → 重新下发短信，倒计时重置为 60 秒。
- 点击"手机号已停用？" → 跳人工申诉表单（TBD-7）。

### 业务规则

- 验证码 6 位数字，5 分钟内有效。
- 同一手机号 24 小时内最多请求 5 次。
- iOS / Android 自动填充短信验证码。

---

## P3. 设置登录密码（子步骤）

### 使用场景

- **注册**：短信验证码通过后，手机号在系统中无账号。
- **忘记密码**：忘记密码流程的最后一步。

### 状态

| 状态     | 触发条件         | 视觉                       |
| ------ | ------------ | ------------------------ |
| 默认     | 进入此子步骤       | 主按钮置灰；两条规则提示均为灰圆圈        |
| 长度满足   | 密码 1 满足 6-20 位 | 第 1 条规则变绿 ✓             |
| 一致性满足  | 密码 1 = 密码 2   | 第 2 条规则变绿 ✓             |
| 可提交    | 两条规则均满足      | 主按钮高亮可点                  |
| 错误     | 提交后服务端报错      | 显示红字"两次密码不一致，请重新输入"      |

### 元素构成

1. **标题**："设置登录密码"。
2. **副标题**：动态显示"为账号 +86 138 0000 0000 设置登录密码，方便下次快速登录"。
3. **密码输入行 1**：占位"请输入新密码（6-20 位）" + 👁 切换显隐。
4. **密码输入行 2**：占位"请再次输入新密码" + 👁。
5. **密码规则提示**（2 条）：
   - 圆圈图标（默认灰圈，满足时变绿✓）+ 文字"6-20 位字母、数字或符号"。
   - 同上 + "两次输入一致"。
6. **错误行**（红字）。
7. **主按钮**（通栏）："完成"。
8. **次链接**（仅注册场景）："跳过，下次再设置"。

### 操作逻辑

- 输入密码 → 实时检查规则，圆圈灰变绿。
- 点击 👁 → 切换密码可见。
- 点击主按钮 → 提交；成功 → 注册流程进 P4 / 忘记密码流程弹轻提示"密码已重置"后回 P1.2。
- 点击"跳过，下次再设置" → 直接进 P4。

### 业务规则

- 密码不能与手机号相同。
- 不强制大小写 / 数字混合。

---

## P-FORGOT. 忘记密码流程

不是独立页面，而是一组复用 P1.1 / P2 / P3 的步骤序列。

### 流程

1. 入口：P1.2 → 点击"忘记密码？"。
2. P1.1 子步骤（输入手机号）。
3. P2 子步骤（验证码）。
4. P3 子步骤（设置新密码）。
5. 完成：弹轻提示"密码已重置" → 自动跳回 P1.2，手机号已自动填入。

### 关键差异（与注册流程相比）

- P3 完成后不进入 P4，而是回 P1.2。
- P3 不显示"跳过，下次再设置"次链接。

---

## P4. 完善个人资料（子步骤）

注册后引导页，所有字段均可"稍后完善"，但昵称为必填。

### 状态

| 状态     | 触发条件               | 视觉                     |
| ------ | ------------------ | ---------------------- |
| 默认     | 注册成功首次进入           | 头像随机一个 emoji；昵称为空；三联表单为空 |
| 昵称为空   | 用户清空了昵称            | 主按钮置灰                  |
| 昵称非空   | 输入了任意昵称            | 主按钮高亮可点                |
| 加载中    | 提交后等待服务端响应         | 主按钮内 spinner             |

### 元素构成

1. **标题**："完善个人资料"。
2. **副标题**："让 AIRing 更懂你 · 资料仅你可见"。
3. **头像区**（居中）：
   - 圆形头像（默认随机 emoji，如 🦊）。
   - 下方按钮"🎲 选择头像"。
4. **昵称行**（必填）：
   - 标签"昵称*"。
   - 输入框（最多 16 字）。
   - 右侧 🎲 随机昵称按钮。
5. **三联表单**（横排 3 列）：
   - 年龄：标签 + 只读输入框 + "岁"后缀（点击触发滚轮）。
   - 身高：同上 + "cm"。
   - 体重：同上 + "kg"。
6. **健康标签区**：
   - 标签"健康标签 (可多选)"。
   - 隐私说明（带🔒）："仅用于为你提供个性化健康分析，**他人不可见**。"
   - 标签 chips 网格：高血压关注 / 糖尿病关注 / 备孕中 / 失眠 / 慢性病 / 减脂 / 增肌 / 孕期 / 哺乳期 / 老年关怀…
7. **主按钮**（通栏）："完成"。
8. **次链接**："稍后完善"。

### 操作逻辑

- 点击头像 / "选择头像" → 弹 P5.2 头像选择器。
- 输入昵称 → 主按钮联动启用 / 禁用。
- 点击 🎲 随机昵称 → 自动填入一个随机昵称。
- 点击年龄 / 身高 / 体重行 → 弹 P5.1 滚轮选择器。
- 点击标签 chip → 切换选中态（蓝填充 / 灰描边）。
- 点击主按钮 → 提交资料 → 弹 P-LOGIN-DONE 轻提示 → 1 秒后自动跳关心 Tab。
- 点击"稍后完善" → 直接跳关心 Tab，弹轻提示"可在个人设置中补充"。

### 业务规则

- 昵称同名允许重复（系统加随机后缀给唯一 ID）。
- 健康标签数量不限。
- 数据加密存储；标签不出现在关心圈。

---

## P5. 个人设置

二级页面，从一级页面顶部头像进入。

### 状态

| 状态     | 触发条件      | 关键差异                    |
| ------ | --------- | ----------------------- |
| 已连接戒指  | 当前账号绑定了戒指 | "我的设备"行显示已连接 SN + 电量    |
| 未连接戒指  | 当前账号无戒指   | "我的设备"行显示"未连接 · 点击搜索附近戒指" |
| 默认     | 各开关均为默认值  | 见各开关默认列                  |

### 元素构成

按区段自上而下：

#### a. 顶栏

- ‹ 返回 + 中标题"个人设置" + 占位。

#### b. 我的设备

- 单行：
  - 左：💍 大图标。
  - 中：主文"AIRing 戒指" + 副文（动态：已连接 / 未连接态）。
  - 右：›。

#### c. 个人资料

5 行设置项：

| 行  | 显示内容                        | 编辑方式               |
| -- | --------------------------- | ------------------ |
| 头像 | 当前头像 emoji + ›              | 弹 P5.2 头像选择器       |
| 昵称 | 当前昵称（未填显示"—"）+ ›             | 弹 P5.3 单字段编辑面板     |
| 年龄 | 当前值（未填显示"未填写"）+ ›            | 弹 P5.1 滚轮选择器（10–100 岁） |
| 身高 | 当前值 + ›                     | 弹 P5.1 滚轮选择器（100–220 cm） |
| 体重 | 当前值 + ›                     | 弹 P5.1 滚轮选择器（30–200 kg，0.1 kg 步进） |

#### d. 健康标签

- 区段头：标题"健康标签" + 「仅你可见」徽章 + 副文。
- 标签 chips（可点击切换）。

#### e. 社交标签（自动生成）

- 区段头：标题"社交标签" + 副文"根据当前挑战自动生成"。
- 标签 chips（不可手动改）：#步数挑战者 / 等。

#### f. 动态发送到关心圈（核心隐私管控）

1. **顶部通告栏**（蓝底，圆角）：
   > 为保护你的身体指标隐私，关心圈**仅展示完成状态与进度**，不展示具体数值与时间细节。
2. **5 项习惯开关**：每项一个独立卡，含


   | 习惯   | 默认 | 仅显示                   | 不显示                |
   | ---- | -- | --------------------- | ------------------ |
   | 走路   | ✓  | 今日步数目标已达成              | 具体步数 / 路线 / 配速     |
   | 睡眠   | ✓  | 睡眠目标完成 + 时长区间（如"7~8h"）| 具体入睡 / 起床时间、深睡比例   |
   | 消耗   | ✗  | "燃脂达 500 千卡"徽章         | 心率、运动时长、具体千卡值      |
   | 吃药打卡 | ✗  | "按时打卡"累计天数            | 药品名称、剂量、服药时间       |
   | 喝水   | ✗  | "饮水目标已达成"             | 具体饮水量 / 时间分布       |

   每个卡含：开关 + 主文 + "仅显示 / 不显示"双行说明 + 内嵌脱敏预览（开关打开时展开）。

#### g. 动态显示范围

- 单行：「谁可以看到我的动态」+ 当前值（"仅亲友"）+ ›。

#### h. 退出登录

- 通栏红色按钮"退出登录"。

### 操作逻辑

- 点击 ‹ 返回 → 回上级页面。
- 点击我的设备行 → 跳 P6。
- 点击个人资料 5 行任一项 → 弹对应编辑器（P5.1 / P5.2 / P5.3）。
- 点击健康标签 chip → 切换选中态。
- 点击 5 项习惯开关 → 即时切换，下方脱敏预览展开 / 收起。
- 点击"动态显示范围"行 → 弹 P5.4 操作菜单。
- 点击"退出登录" → 弹 P5.5 确认框。

### 业务规则

- 个人资料字段编辑后实时持久化。
- 5 项习惯开关默认值如表中所示。

---

## P5.1. 滚轮选择器（年龄 / 身高 / 体重）

### 状态

仅 1 种状态（按字段不同显示不同滚轮内容）。

### 元素构成

1. **顶栏**：
   - 左：取消（文字按钮）。
   - 中：标题（动态："选择年龄" / "选择身高" / "选择体重"）。
   - 右：完成（文字按钮，主蓝高亮）。
2. **滚轮主体**：
   - 行高 36px，scroll snap。
   - 中间高亮一行（带边框）。
   - 上下渐变遮罩。
   - 右侧固定单位文字（"岁" / "cm" / "kg"）。

### 操作逻辑

- 滚动滚轮 → 实时切换中间高亮值。
- 点击"取消" → 关闭，丢弃。
- 点击"完成" → 把高亮值回写到 P5 / P4 对应字段，关闭。

---

## P5.2. 头像选择器

底部弹层，5×4 网格选择默认头像。

### 状态

| 状态  | 触发条件      | 视觉              |
| --- | --------- | --------------- |
| 默认  | 进入        | 当前头像高亮（带勾或边框）   |
| 已选中 | 点击其他 emoji | 新选中项高亮，旧项恢复     |

### 元素构成

1. **顶部 Header**：标题"选择头像"。
2. **5×4 网格**：20 个默认 emoji 头像，每格圆形容器。
3. **底部按钮组**（双按钮）：取消 / 确定。

### 操作逻辑

- 点击网格中任一 emoji → 切换选中态。
- 点击"取消" → 关闭，丢弃。
- 点击"确定" → 把选中头像写回 P5 / P4，关闭。

> TBD：是否支持上传自定义图片头像（涉及内容审核）。

---

## P5.3. 单字段编辑面板

底部弹层，编辑昵称 / 备注等单一文本字段。

### 状态

| 状态  | 触发条件        | 视觉              |
| --- | ----------- | --------------- |
| 默认  | 进入          | 输入框带当前值，光标已激活    |
| 错误  | 不满足校验（如昵称为空） | 输入框红边 + 红字提示     |

### 元素构成

1. **顶部**：标题（动态："编辑昵称" / "编辑备注"）。
2. **输入区**：
   - 单行输入框 + 右侧 ✕ 一键清空。
   - 单位后缀（如有）。
3. **底部按钮组**：取消 / 保存。

### 操作逻辑

- 输入文字 → 实时校验长度。
- 点击"取消" → 关闭，丢弃。
- 点击"保存" → 校验通过则写回 P5，关闭；不通过则显示红字提示。

### 业务规则

- 昵称：≤ 16 字、不能为空。
- 备注：≤ 8 字。

---

## P5.4. 动态显示范围

操作菜单。

### 状态

仅 1 种状态。当前选中项右侧带 ✓。

### 元素构成

1. **选项组**：
   - 仅亲友（默认推荐）。
   - 亲友 + 关注的好友。
   - 仅自己（不分享）。
2. **取消组**（独立）：取消按钮。

### 操作逻辑

- 点击任一选项 → 把选中值写回 P5 显示范围行 → 关闭。
- 点击"取消" → 关闭，不修改。

### 业务规则

- 该值与《02-关心模块.md》C6 关心设置中的"动态可见性"是同一份数据。

---

## P5.5. 退出登录二次确认

iOS 风格确认框，破坏性操作。

### 状态

仅 1 种状态。

### 元素构成

| 字段  | 文案                              |
| --- | ------------------------------- |
| 标题  | 退出当前账号                          |
| 内容  | 退出后将不再接收提醒，下次需重新登录。确认要退出吗？      |
| 左按钮 | 取消                              |
| 右按钮 | 确认退出（红色破坏性）                     |

### 操作逻辑

- 点击"取消" → 关闭。
- 点击"确认退出" → 清空本地登录凭据 + 用户数据缓存 → 关闭确认框 → 跳到未登录态的"今日"Tab。

---

## P6. 连接戒指页

二级页面，承担"找戒指 → 连接 → 看设备 → 移除"完整闭环。

> 唯一入口收口：今日首页"未连接戒指卡"的「立即连接戒指」按钮、个人设置 P5 我的设备行均跳转到本页。

### 状态

| 状态   | 触发条件            | 关键差异         |
| ---- | --------------- | ------------ |
| 已连接  | 当前账号已绑定一枚戒指     | 显示 P6.1 内嵌区段 |
| 未连接  | 当前账号无戒指 / 主动移除后 | 显示 P6.2 内嵌区段 |

### 元素构成

1. **顶栏**：‹ 返回（左）+ 中标题"戒指" + 占位（右）。
2. **主内容区**：根据状态渲染 P6.1 或 P6.2。

### 操作逻辑

- 点击 ‹ → 回上级页面。

---

### P6.1. 已连接态（内嵌区段）

#### 状态

| 状态     | 触发条件             | 视觉                       |
| ------ | ---------------- | ------------------------ |
| 在线     | 戒指实时连接           | 状态点绿 + "已连接"             |
| 离线     | 戒指失联 > 5 分钟      | 状态点灰 + "● 离线 · 上次同步 X 分钟前" |
| 电量低    | 电量 < 20%         | 副信息追加红字"电量不足，请尽快充电"      |

#### 元素构成

1. **戒指渲染图**（大图，居中展示）。
2. **设备信息卡**（白底圆角）：
   - 戒指名称："AIRing 戒指"（16/700）。
   - 序列号示例："AR-XXXX-XXXX"。
   - 电量百分比 + 电量条（带颜色指示：> 50% 绿 / 20-50% 黄 / < 20% 红）。
   - 连接状态：● + 文字。
   - 固件版本（可选）：示例"v1.2.3"。
   - 上次同步时间："刚刚 / N 分钟前"。
3. **移除按钮**（红色文字按钮，居中）："移除戒指"。

#### 操作逻辑

- 点击"移除戒指" → 弹 P6.3 确认框。

---

### P6.2. 搜索 / 配对态（内嵌区段）

#### 状态

| 状态  | 触发条件          | 视觉                       |
| --- | ------------- | ------------------------ |
| 搜索中 | 进入页面 / 点击重新搜索 | 旋转动画 + "正在搜索附近的戒指…"     |
| 已发现 | 找到 ≥ 1 枚戒指    | ✓ + "已发现 N 枚戒指" + 列表展示    |
| 无结果 | 30 秒搜不到       | 📡 + "未发现戒指" + "重新搜索"按钮 |
| 蓝牙关  | 用户拒绝蓝牙授权      | 列表区显示"请开启蓝牙后重试"按钮       |
| 连接中  | 点击列表项的"连接"    | 该项按钮变 spinner + "连接中…"  |

#### 元素构成

1. **顶部说明**："请确保戒指已充电并贴近手机"。
2. **搜索状态指示**（按状态）：旋转动画 + 文字 / ✓ + 文字 / 📡 + 文字。
3. **附近戒指列表**：每条
   - 左：戒指渲染图。
   - 中：名称（如"AIRing 戒指 ·"） + 信号强度（- - / 弱 / 中 / 强）。
   - 右：主按钮"连接"。
4. **底部链接**："连接遇到问题？查看帮助"。

#### 操作逻辑

- 点击列表项"连接" → 蓝牙握手 → 成功弹 P-CONNECT-DONE 轻提示，状态切换为 P6.1 已连接态；失败弹轻提示"连接失败，请重试"，按钮恢复。
- 点击"重新搜索"按钮 → 重新启动蓝牙扫描。
- 点击"请开启蓝牙后重试" → 跳系统蓝牙授权弹窗。

#### 业务规则

- 同一账号已绑定 3 枚戒指上限时，连接动作前先弹确认框"已达到 3 枚上限，请先解绑其他戒指"。
- 蓝牙搜索超时 30 秒，自动停止。

---

### P6.3. 移除戒指二次确认

iOS 风格确认框，破坏性操作。

#### 状态

仅 1 种状态。

#### 元素构成

| 字段  | 文案                                          |
| --- | ------------------------------------------- |
| 标题  | 移除戒指                                        |
| 内容  | 移除后将无法继续检测心率、HRV、睡眠等身体指标。已记录的历史数据不会丢失。      |
| 左按钮 | 取消                                          |
| 右按钮 | 移除（红色破坏性）                                   |

#### 操作逻辑

- 点击"取消" → 关闭。
- 点击"移除" → 调用解绑接口；本地清理蓝牙记录与缓存数据 → 弹轻提示"已移除戒指" → 页面切回 P6.2 → 全局：今日首页 C2 区域切回"戒指未连接卡"，P5 我的设备行副信息变成"未连接 · 点击搜索附近戒指"。

---

## P-LOGIN-DONE. 登录成功轻提示

全屏遮罩 + 居中卡片，1 秒自动消失。

### 状态

仅 1 种状态。

### 元素构成

- ✓ 大图标（绿色）。
- 主文"登录成功"。
- 副文"即将进入「关心」页…"。

### 操作逻辑

- 1 秒后自动消失：
  - 首次注册 → 跳关心 Tab。
  - 已注册回归 → 跳上次所在 Tab。

---

## P-CONNECT-DONE. 连接成功轻提示

全屏遮罩 + 居中卡片，1 秒自动消失。

### 状态

仅 1 种状态。

### 元素构成

- ✓ 大图标（绿色）。
- 主文"戒指已连接"。
- 副文"1 秒后回到首页"。

### 操作逻辑

- 1 秒后自动消失：
  - 入口为 P5（个人设置） → 留在 P6 已连接态。
  - 入口为今日首页未连接卡 → 自动跳回今日首页，三环开始拉取实时数据。

---

## 业务规则汇总

### 账号体系

- 一手机号 = 一账号；不支持多手机号绑定。
- 注销账号：本期不在 P5 中提供，后续接入"账号设置 → 注销"二级页（TBD-3）。
- 设备绑定：一账号最多绑 3 个戒指，超出需先解绑。

### 数据存储与安全

- 个人资料（头像 / 昵称 / 年龄 / 身高 / 体重 / 健康标签）：服务端存储，端到端加密。
- 健康标签**仅参与服务端的健康推荐算法**，不会同步到关心圈、不会用于推荐"附近的人"。
- 密码：加盐哈希存储，永不明文回传。
- 登录凭据：1 天过期 + 30 天刷新机制。

### 协议与合规

- 用户协议、隐私政策为外链 H5 页（应用内浏览器）。
- 14 岁以下用户禁止注册（手机号注册时无法识别，主要靠出生年龄校验：> 14 岁才允许填年龄）。
- iOS 端遵循透明度规范，定位、通知、健康数据访问需独立权限弹窗。

---

## 待办 / 未确认（TBD）

| 编号       | 描述                                    |
| -------- | ------------------------------------- |
| P-TBD-1  | 是否需要"第三方登录"（微信 / Apple ID）？---暂不支持    |
| P-TBD-2  | 头像选择器是否支持上传自定义图片？---暂不支持              |
| P-TBD-3  | 注销账号入口、未成年人保护机制（家长模式）何时上线---一期不醉哦     |
| P-TBD-4  | 多设备登录策略：同账号最多几台手机同时在线？挤下旧设备的提示文案      |
| P-TBD-5  | 健康标签的列表需医学顾问确认完整性                     |
| P-TBD-6  | 身高 / 体重单位切换（cm/inch、kg/lb）是否支持，本期默认公制 |
| P-TBD-7  | 「忘记密码」中"手机号已停用"链接的承接方案（人工申诉表单？）       |
| P-TBD-8  | 戒指 OTA 固件升级是否在 P6 提供入口？升级中的状态展示与失败回滚  |
| P-TBD-9  | 多枚戒指同账号场景的"主戒指 / 副戒指"切换 UI（本期默认仅 1 枚） |
| P-TBD-10 | 移除戒指后是否提供"30 天保留蓝牙记录，下次自动重连"选项        |
